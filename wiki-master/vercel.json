{
    "builds": [{
        "src": "encyclopedia/wsgi.py",
        "use": "@vercel/python"
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "encyclopedia/wsgi.py"
        }
    ]
}
